VITE_BRIDGE_BASE_URL=http://127.0.0.1:17890
# Enable sandbox write pilots in schedule + patient profile (disposable data only).
# VITE_SANDBOX_WRITE_PILOT=true
# Legacy alias (status pilot only): VITE_APPOINTMENT_STATUS_WRITE_PILOT=true
